server:
  port: 1001

spring:
  profiles:
    active: dev

  application:
    name: eureka-server

eureka:
  instance:
    hostname: localhost
  #禁止注册自己
  client:
    register-with-eureka: false
    fetch-registry: false